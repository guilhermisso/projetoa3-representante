function votar() {
    // Obter o valor selecionado (opcional para usar o valor depois)
    const candidato = document.getElementById("candidato").value;

    // Redirecionar para a página de confirmação
    window.location.href = "fim.html";
}
